NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# browser (default) keeps Play state/rules/history local; server restores the
# legacy fully server-owned hand runtime for parity debugging.
NEXT_PUBLIC_PLAY_RUNTIME=browser
